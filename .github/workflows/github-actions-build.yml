name: Build Server
on: [push]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@master
            - uses: actions/setup-go@v1
              with:
                go-version: '1.20.4'
            - run: make deps
            - run: make server/build